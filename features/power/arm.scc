# SPDX-License-Identifier: MIT
define KFEATURE_DESCRIPTION "Enable ARM/ARM64 Power Management options"
define KFEATURE_COMPATIBILITY board

kconf non-hardware arm_common.cfg

# 32 bit arm
if [ "$KARCH" = "arm" ]; then
	kconf hardware arm.cfg
fi
